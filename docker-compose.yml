version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    network_mode: host
    cap_add:
      - NET_RAW
      - NET_ADMIN
    environment:
      - SNIFF_INTERFACE=eth0

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
